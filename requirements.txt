fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
httpx[http2]>=0.27
beautifulsoup4>=4.12
lxml>=5.0
apscheduler>=3.10,<4
pydantic>=2.7
python-dotenv>=1.0
# экспорт каталога и контактов в Excel: без него /api/export?format=xlsx
# отвечал 500 на чистой установке (test_api_roundtrip)
openpyxl>=3.1
# провайдеры перевода (все необязательные — включаются настройкой)
anthropic>=1.0
# проверка
pytest>=8.0
# фолбэк для источников за анти-ботом (страницу открывает настоящий Chromium)
playwright>=1.45
